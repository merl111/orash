#ifndef __CONFIG_H
#define __CONFIG_H

const char* hist_path = "/tmp/orash_history";
const int hist_length = 500;
const char* prompt = "orash> "; // for now a const, should be more dynamic in the future

#endif /* __CONFIG_H */
